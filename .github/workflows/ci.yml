name: CI
on:
  push:
    branches:
      - '**'
      - '!integrated/**'
      - '!stl-preview-head/**'
      - '!stl-preview-base/**'
      - '!generated'
      - '!codegen/**'
      - 'codegen/stl/**'
  pull_request:
    branches-ignore:
      - 'stl-preview-head/**'
      - 'stl-preview-base/**'
  # The vulnerability database changes even when this repository does not.
  # This schedule runs only the govulncheck job below; lint and tests remain
  # push-triggered. 10:23 UTC avoids GitHub's busy top-of-hour window.
  schedule:
    - cron: '23 10 * * *'

permissions:
  contents: read

jobs:
  build:
    timeout-minutes: 10
    name: build
    permissions:
      contents: read
      id-token: write
    runs-on: ${{ github.repository == 'stainless-sdks/openai-go' && 'depot-ubuntu-24.04' || 'ubuntu-latest' }}
    if: |-
      github.repository == 'stainless-sdks/openai-go' &&
      github.event_name == 'push' &&
      github.event.head_commit.message != 'codegen metadata'
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Get GitHub OIDC Token
        if: |-
          github.repository == 'stainless-sdks/openai-go' &&
          github.event_name == 'push' &&
          !startsWith(github.ref, 'refs/heads/stl/')
        id: github-oidc
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          script: core.setOutput('github_token', await core.getIDToken());

      - name: Upload tarball
        if: |-
          github.repository == 'stainless-sdks/openai-go' &&
          github.event_name == 'push' &&
          !startsWith(github.ref, 'refs/heads/stl/')
        env:
          URL: https://pkg.stainless.com/s
          AUTH: ${{ steps.github-oidc.outputs.github_token }}
          SHA: ${{ github.sha }}
        run: ./scripts/utils/upload-artifact.sh
  lint:
    timeout-minutes: 10
    name: lint
    runs-on: ${{ github.repository == 'stainless-sdks/openai-go' && 'depot-ubuntu-24.04' || 'ubuntu-latest' }}
    # Run on the PR event even for same-repository branches. The stable
    # aggregate check at the bottom cannot safely depend on a separate push
    # workflow run, and every PR should receive the same merge-gating signal.
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    # Prevent Go's automatic toolchain selection from hiding an accidental
    # increase above the minimum declared in go.mod.
    env:
      GOTOOLCHAIN: local

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          # go.mod is the source of truth for the minimum compiler used by lint.
          go-version-file: ./go.mod

      - name: Run lints
        run: ./scripts/lint

      - name: Check go.mod is tidy
        run: ./scripts/check-go-mod
  test:
    timeout-minutes: 15
    name: test (Go ${{ matrix.go-version }})
    runs-on: ${{ github.repository == 'stainless-sdks/openai-go' && 'depot-ubuntu-24.04' || 'ubuntu-latest' }}
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    strategy:
      fail-fast: false
      matrix:
        # Test the latest patch in the previous and current stable Go lines.
        # Keep this matrix synchronized with GO_VERSION_POLICY.md and
        # .github/go-support-policy.json.
        go-version:
          - '1.25.x'
          - '1.26.x'
    # setup-go installs the selected matrix version first. "local" then stops
    # the go command from silently switching to a newer toolchain.
    env:
      GOTOOLCHAIN: local
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ matrix.go-version }}

      - name: Bootstrap
        env:
          SKIP_BREW: "1"
        run: ./scripts/bootstrap

      - name: Test root module
        run: ./scripts/test

      - name: Test examples module
        working-directory: examples
        run: go test ./...

      - name: Test external consumer
        working-directory: internal/testdata/consumer
        # This nested module catches accidental reliance on internal packages
        # and proves representative core and Azure imports compose externally.
        # readonly also prevents a disposable CI checkout from hiding stale
        # module metadata by rewriting go.mod or go.sum during the test.
        run: go test -mod=readonly ./...

  vulnerability:
    timeout-minutes: 15
    name: govulncheck
    runs-on: ${{ github.repository == 'stainless-sdks/openai-go' && 'depot-ubuntu-24.04' || 'ubuntu-latest' }}
    # PR scans catch dependency/call-graph changes. The nightly scan catches
    # newly published advisories against an otherwise unchanged commit.
    if: github.event_name == 'pull_request' || github.event_name == 'schedule'
    env:
      GOTOOLCHAIN: local
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup current stable Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          # Scan with a patched current toolchain so old standard-library
          # findings do not obscure dependency findings.
          go-version: '1.26.x'

      - name: Install govulncheck
        # Keep the scanner in its own module so Dependabot can update it without
        # adding development-tool dependencies to SDK users' module graph.
        working-directory: tools
        run: go install golang.org/x/vuln/cmd/govulncheck

      - name: Scan root module
        run: govulncheck ./...

      - name: Scan examples module
        working-directory: examples
        run: govulncheck ./...

  go-version-change:
    timeout-minutes: 5
    name: Go version change policy
    runs-on: ubuntu-latest
    # This check needs both the PR base SHA and PR description, so it runs only
    # for pull_request events rather than the duplicate branch-push event.
    if: github.event_name == 'pull_request'
    env:
      GOTOOLCHAIN: local
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          # The checker compares go.mod at the base SHA with the PR head.
          fetch-depth: 0
          persist-credentials: false

      - name: Setup go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: ./go.mod

      - name: Check required policy artifacts
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          # Pass untrusted PR text through the environment, never interpolate
          # it into the shell command.
          PR_BODY: ${{ github.event.pull_request.body }}
        run: go run ./scripts/check-go-version-change "$BASE_SHA"

      - name: Check generated release communication
        # The approved release note lives in go-support-policy.json. Generated
        # release PRs must carry it into CHANGELOG.md before they can merge.
        if: startsWith(github.head_ref, 'release-please--')
        run: go run ./scripts/check-go-release-note

  required:
    timeout-minutes: 5
    name: Go support policy
    runs-on: ubuntu-latest
    if: always() && github.event_name == 'pull_request'
    needs:
      - lint
      - test
      - vulnerability
      - go-version-change
    steps:
      - name: Require every Go safety job
        env:
          LINT_RESULT: ${{ needs.lint.result }}
          TEST_RESULT: ${{ needs.test.result }}
          VULNERABILITY_RESULT: ${{ needs.vulnerability.result }}
          VERSION_POLICY_RESULT: ${{ needs.go-version-change.result }}
        run: |
          failed=0
          for result in \
            "$LINT_RESULT" \
            "$TEST_RESULT" \
            "$VULNERABILITY_RESULT" \
            "$VERSION_POLICY_RESULT"
          do
            if [[ "$result" != "success" ]]; then
              echo "Required Go safety job ended with: $result"
              failed=1
            fi
          done
          exit "$failed"
