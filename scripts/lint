#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

echo "==> Running Go build"
go build ./...

echo "==> Running golangci-lint"
golangci-lint run ./...

echo "==> Checking tests compile"
go test -run=^$ ./...
